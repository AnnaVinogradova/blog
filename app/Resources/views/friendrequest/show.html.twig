{% extends 'base.html.twig' %}

{% block body %}
    <h1>FriendRequest</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ friendRequest.id }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{% if friendRequest.status %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('friendrequest_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('friendrequest_edit', { 'id': friendRequest.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
