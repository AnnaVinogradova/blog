{% extends 'base.html.twig' %}

{% block body %}
    <div id="content">
        <h3>MapResolver creation</h3>
        
        <div class="edit-form">
            {{ form_start(form) }}
                {{ form_widget(form) }}
                <input type="submit" value="Create" />
            {{ form_end(form) }}
        </div>
        
        <ul>
            <li>
                <a href="{{ path('mapresolver_index', { 'id': id }) }}">Back to the list</a>
            </li>
        </ul>
    </div>
{% endblock %}
