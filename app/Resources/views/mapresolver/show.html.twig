{% extends 'base.html.twig' %}

{% block body %}
<div id="content">
    <h3>MapResolver</h3>
    User: {{ mapResolver.user }}
    
    <ul>
        <li class="edit-form">
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
        <li>
            <a href="{{ path('mapresolver_index', {'id' : mapResolver.map.id}) }}">Back to the list</a>
        </li>
    </ul>
</div>
{% endblock %}
